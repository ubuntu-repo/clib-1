#ifndef _STRING_H
#define _STRING_H

///Get the length of a \0 terminated string
int cstring_length(char *a);

///Creates a new string of a + b
char *cstring_concat(char *a, char *b);

#endif /* _STRING_H */
